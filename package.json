{
  "name": "formation",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start:client:dev": "cd client && npm run start",
    "start:server:dev": "node --inspect ./server/bin/www",
    "build:client": "cd client && npm run build",
    "start:prod": "npm run build:client && node ./server/bin/www"
  }
}
